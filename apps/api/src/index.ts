// matchday REST API — Hono on Cloudflare Workers, REST + OpenAPI via @hono/zod-openapi.
// Reaches Neon Postgres through the serverless driver / Hyperdrive (never raw pg TCP).
// Routes, OpenAPI, and Neon wiring land in docs/todo.md Phase 4.

export {};
