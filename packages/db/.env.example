# Neon Postgres connection string (pooled). Used by drizzle-kit migrations and db scripts.
# Reached from Workers via the serverless (neon-http) driver, never raw pg TCP.
DATABASE_URL=postgres://user:password@host/dbname
